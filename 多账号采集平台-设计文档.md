# 多账号视频采集平台 — 设计文档

> 版本：v0.3（评审稿，已并入历史会话成果 + 人工接管硬约束）
> 日期：2025-08
> 目标：GUI 界面 + 比特浏览器（BitBrowser）多账号 + 多平台（抖音/小红书/B站）视频与评论采集
>
> 📌 **v0.3 变更**：固化「人工接管」机制（P7 硬约束）：
> ① 新设计原则 P7/P8（1.2 节）—— 出现登录/验证码时 GUI 提示是哪个浏览器，冻结页面等用户通知，**通知前不刷新、不关闭**
> ② 账号状态机新增 `waiting_human` 冻结态（5.4 节）+ 冻结规则（5.5 节）
> ③ GUI 人工接管交互设计：悬浮警示条 + 弹窗 + 系统通知 + 「继续」按钮（6.3 节）
> ④ BitBrowser 对接层冻结约束（4.2/4.3 节）—— 冻结期间禁止 stop/close/navigate
> ⑤ 数据库：accounts 表加 wait_* 字段，新增 human_actions 审计表（3.2 节）
>
> 📌 **v0.2 变更**：参考上一会话（`dsh-session-session-c9124011-...zip`）的完整采集实践，新增：
> ① 历史资产盘点（第 1.4 节，`F:\DSH\cdp\` 已有 13 关键词批量搜索 + 评论采集 + 意向分析 + 用户聚合全套脚本）
> ② 意向分析模块（第 7A 章）—— 复用上次的 `intent-rules.json` 规则体系
> ③ CSV 导出规范（第 7B 章）—— 修复上次的乱码问题（UTF-8 BOM）
> ④ 历史踩坑清单（第 14 章）—— 蓝屏/内存、搜索翻页少、验证码人工辅助等

---

## 0. 文档目的与阅读顺序

按你的要求，本平台**按平台逐个确定采集流程**，最后统一整合。本文档结构：

1. **第一部分（1~7章）**：全局架构、技术选型、数据库、任务调度——一次定死，三平台共用
2. **第二部分（8~10章）**：抖音 → 小红书 → B站 各自的采集流程细节（可逐个评审确认）
3. **第三部分（11~12章）**：统一整合后的完整流程、GUI 设计、实施路线图

> ✅ 评审建议：先过 1~7 章（整体骨架），再逐章过 8/9/10（平台细节），最后确认 11 章整合方案。

---

## 1. 项目概述

### 1.1 需求原文（转述）

- 提供一个 **GUI 界面**，通过界面操作**比特浏览器（BitBrowser）**
- 界面中填写**关键词**、选择**平台**（抖音、B站、小红书等）
- 自动采集该关键词下的**所有视频/笔记**（内容尽量全面）
- 采集流程分两阶段：
  - **阶段 A（搜索）**：用一个账号把所有视频 URL 采集完
  - **阶段 B（评论）**：URL 平均分给 N 个账号，各账号并行提取评论
- **冷却机制**：每个账号提取 **10 个 URL 的评论后，暂停操作 1 分钟**
- 平台采集脚本需重新编写（不复用现有单平台技能）
- **（来自上会话）意向分析**：对评论按关键词规则打分（高/中/低意向），生成意向用户 CSV
- **（来自上会话）用户聚合**：对高意向用户按 uid 去重聚合，输出用户级线索表
- **（来自上会话）多关键词批量**：一次任务支持 5~15 个关键词批量搜索、跨关键词去重合并

### 1.2 设计原则

| 原则 | 说明 |
|---|---|
| P1 平台隔离 | 每个平台独立采集器模块，互不干扰，可单独启用/停用 |
| P2 账号与平台绑定 | 抖音账号只能采抖音，小红书账号只能采小红书 |
| P3 全量落盘 | 任何中间结果（URL、评论、任务状态）实时写入 SQLite，断电可续 |
| P4 风控优先 | 一切节奏参数（冷却、配额、并发）可配置，默认保守 |
| P5 人工兜底 | 登录态失效、验证码、滑块 → 暂停该账号并 GUI 提醒人工处理 |
| P6 可观测 | GUI 实时显示：任务进度、账号状态、采集计数、错误日志 |
| **P7 人工接管（硬约束）** | 出现登录/验证码/滑块等需人工操作时：① GUI **醒目提示是哪个浏览器（profile X）**；② 该账号进入 `waiting_human` 状态，**冻结当前页面不动**；③ **在用户明确通知「继续」之前，禁止刷新、禁止关闭、禁止导航该浏览器**；④ 用户完成操作后点击「继续」，系统从断点恢复该账号任务 |
| **P8 多账号互不阻塞** | 某账号处于 `waiting_human` 时，其他账号正常继续采集，不受影响 |

### 1.3 范围边界

| 做 | 不做 |
|---|---|
| ✅ 关键词搜索采集视频/笔记 URL | ❌ 视频文件/无水印视频下载 |
| ✅ 采集评论（含二级评论，视平台能力） | ❌ 发帖、评论、点赞等任何写操作 |
| ✅ 多账号并行 + 冷却 + 配额 | ❌ 自动过验证码/滑块（只提醒人工） |
| ✅ 断点续采 + 去重 | ❌ 代理自动购买/管理（仅接入已有代理） |
| ✅ 数据导出（JSON/CSV/Excel） | ❌ 商业化使用（注意各平台与工具 ToS） |
| ✅ **人工接管流程**（P7：提示→冻结→等通知→恢复） | ❌ 未经用户通知就刷新/关闭/导航被冻结的浏览器 |

### 1.4 历史资产盘点（上一会话成果，直接复用）

上一会话已用 CDP 直连桌面 Chrome 跑通**抖音单账号全流程**，`F:\DSH\cdp\` 下的脚本是现成的算法/经验资产：

| 文件 | 功能 | 本平台复用方式 |
|---|---|---|
| `batch-search.ps1` | **13 个关键词批量搜索**，按视频 ID 去重合并 | 算法复刻 → 搜索阶段 |
| `collect-search.ps1` | 单关键词搜索采集（滚动加载 + 抓 API） | 算法复刻 → 搜索采集器 |
| `collect-comments.ps1` | 逐视频滚动加载评论，抓 `/comment/list/` API，**每 10 个暂停 20s** | 冷却算法已实际验证 → 升级为 60~90s 随机 |
| `analyze-comments.ps1` | 按 `intent-rules.json` 打分 → 意向 CSV | 意向分析模块（第 7A 章） |
| `dedupe-leads.ps1` | 高意向用户按 uid 聚合去重 | 用户聚合模块（第 7A 章） |
| `intent-rules.json` | 高/中/低意向关键词规则 + 回复模板 | 直接复用，GUI 可编辑 |
| `douyin-intent-leads.csv` | 意向用户明细（上次产出物） | 导出格式模板（第 7B 章） |
| `douyin-user-leads.csv` | 聚合后的用户级线索（上次产出物） | 导出格式模板（第 7B 章） |

> ⚠️ **重要差异**：上次是**单账号 + CDP 直连桌面 Chrome**；本平台改为**多账号 + BitBrowser profile**。核心算法（搜索/评论/打分/聚合）不变，**驱动层替换**为 BitBrowser CDP 对接，**并发模型**从串行升级为 N 账号并行。
>
> 📂 上次产出物示例（可作验收基准）：
> - 搜索合并：`search-results-all.json`（13 关键词去重后结果）
> - 意向明细表头：`用户ID,用户主页,评论内容,意向分析评分,回复建议,昵称,评论时间,来源作品,作品链接`
> - 用户聚合表头：`用户ID,昵称,用户主页,高意向评论数,评论总条数,最新评论时间,最早评论时间,涉及作品数,高意向评论摘录,涉及作品列表`

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        GUI（用户操作层）                       │
│   Electron 前端  ←→  Python 后端（FastAPI WebSocket）        │
│   关键词/平台/账号/冷却参数配置  ·  任务创建  ·  实时进度监控   │
└───────────────────────────┬─────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  任务调度器（核心编排层）                       │
│  · 任务状态机（pending→running→done/failed/cooldown）        │
│  · 阶段A：搜索任务分配（主账号）                               │
│  · 阶段B：评论任务分片（URL均分 N 账号）                       │
│  · 冷却调度器（每10 URL 停 60~90s 随机）                      │
│  · 配额控制器（每账号每日上限）                               │
└───────────┬──────────────────────────────┬──────────────────┘
            ▼                              ▼
┌──────────────────────────┐   ┌──────────────────────────────┐
│    BitBrowser 对接层      │   │       存储层                  │
│  API 打开 profile → 返回  │   │  SQLite：videos / comments /  │
│  CDP ws 端口 → Playwright │   │  tasks / accounts / configs  │
│  connect → 驱动采集器     │   │  导出：JSON / CSV / Excel     │
└──────────────────────────┘   └──────────────────────────────┘
            ▼
┌─────────────────────────────────────────────────────────────┐
│                  平台采集器（可插拔）                          │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 抖音     │  │ 小红书   │  │ B站     │  │ (预留)   │        │
│  │ douyin  │  │ xhs     │  │ bilibili│  │ 快手/微博│        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│  统一接口：search(keyword) / fetch_comments(url, 分片参数)    │
└─────────────────────────────────────────────────────────────┘
```

### 2.1 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| GUI 前端 | **Electron + Vue/React** | 成熟、跨平台、可打包成桌面应用 |
| 后端 | **Python 3.11 + FastAPI + WebSocket** | 与 MediaCrawler（Python）无缝集成 |
| 浏览器驱动 | **Playwright (Python)** | MediaCrawler 已基于它，直接复用 |
| 指纹浏览器 | **BitBrowser**（免费 10 profile） | 通过其本地 API 打开 profile，拿到 CDP 端口后 Playwright 连接 |
| 数据库 | **SQLite + SQLAlchemy** | 单机零运维，满足并发读写（写多读少加 WAL） |
| 任务队列 | **内置调度器**（无需 Celery/Redis） | 单机 5~10 账号规模，避免引入消息中间件复杂度 |
| 采集引擎 | **MediaCrawler（本地已有克隆）二次开发** | 已支持三平台搜索+评论+登录态缓存+代理池，30K star 验证 |

> ⚠️ **关键决策**：不自己从零写爬虫。在 MediaCrawler 基础上包一层「任务接口 + BitBrowser 对接 + 冷却调度」，工作量集中在编排层而非逆向层。

---

## 3. 数据库设计（SQLite）

### 3.1 ER 概览

```
accounts(账号表) 1 ──── N tasks(任务表) 1 ──── N task_items(任务明细)
                        │
                        └─── N videos(视频表) 1 ──── N comments(评论表)
```

### 3.2 表结构

```sql
-- 账号表（对应 BitBrowser 的一个 profile）
CREATE TABLE accounts (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    name        TEXT UNIQUE NOT NULL,          -- 账号名（如 dy_01）
    platform    TEXT NOT NULL,                 -- douyin / xhs / bilibili
    bit_profile TEXT NOT NULL,                 -- BitBrowser profile id
    status      TEXT DEFAULT 'ready',          -- ready/busy/cooldown/waiting_human/disabled
    wait_reason TEXT,                          -- waiting_human 原因：login/captcha/other
    wait_prompt TEXT,                          -- 给用户的提示文案（含浏览器名）
    wait_since  TIMESTAMP,                     -- 进入 waiting_human 的时间
    wait_current_url TEXT,                     -- 冻结时的当前URL（恢复时断点续采）
    daily_quota INTEGER DEFAULT 0,             -- 今日已采集URL数
    daily_limit INTEGER DEFAULT 200,           -- 每日上限（可配置）
    last_active TIMESTAMP,
    remark      TEXT
);

-- 人工接管事件表（审计：谁、何时、为何冻结，何时恢复）
CREATE TABLE human_actions (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id  INTEGER NOT NULL REFERENCES accounts(id),
    action      TEXT NOT NULL,                 -- freeze / resume
    reason      TEXT,                          -- login / captcha / other
    prompt      TEXT,                          -- 提示文案
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 任务表（一个「关键词 × 平台」= 一个总任务）
CREATE TABLE tasks (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    platform    TEXT NOT NULL,
    keyword     TEXT NOT NULL,
    status      TEXT DEFAULT 'pending',        -- pending/searching/commenting/done/failed
    max_pages   INTEGER DEFAULT 0,             -- 搜索翻页上限（0=平台默认）
    total_videos   INTEGER DEFAULT 0,          -- 阶段A找到的URL总数
    commented      INTEGER DEFAULT 0,          -- 已完成评论的URL数
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    finished_at TIMESTAMP,
    UNIQUE(platform, keyword)                  -- 同关键词同平台不重复建任务
);

-- 视频表（阶段A产物：去重后的视频/笔记 URL）
CREATE TABLE videos (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id     INTEGER NOT NULL REFERENCES tasks(id),
    platform    TEXT NOT NULL,
    video_id    TEXT NOT NULL,                 -- 平台内唯一ID
    url         TEXT NOT NULL,
    title       TEXT,
    author      TEXT,
    publish_time TEXT,
    extra       TEXT,                          -- JSON：点赞数、播放数等平台特有字段
    comment_status TEXT DEFAULT 'pending',     -- pending/assigned/done/failed
    assigned_account INTEGER REFERENCES accounts(id),  -- 分给哪个账号
    UNIQUE(platform, video_id)                 -- 跨任务全局去重
);

-- 评论表（阶段B产物）
CREATE TABLE comments (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    video_id    INTEGER NOT NULL REFERENCES videos(id),
    comment_id  TEXT,                          -- 平台内评论ID
    username    TEXT,
    content     TEXT,
    likes       INTEGER DEFAULT 0,
    comment_time TEXT,
    parent_id   TEXT,                          -- 二级评论的父评论ID
    extra       TEXT,                          -- JSON：平台特有字段
    intent_score INTEGER,                      -- 意向评分 5/3/1（analyze 阶段填）
    intent_label TEXT,                         -- 高意向/中意向/低意向
    reply_suggestion TEXT,                     -- 回复建议模板
    UNIQUE(video_id, comment_id)
);

-- 意向规则表（GUI 可编辑，替代 intent-rules.json）
CREATE TABLE intent_rules (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    level       TEXT NOT NULL,                 -- high/medium/low
    score       INTEGER NOT NULL,              -- 5/3/1
    keyword     TEXT NOT NULL,                 -- 命中关键词
    template    TEXT,                          -- 该级别的回复模板
    UNIQUE(level, keyword)
);

-- 任务明细表（阶段B的分片结果：哪个账号负责哪些URL）
CREATE TABLE task_items (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id     INTEGER NOT NULL,
    video_id    INTEGER NOT NULL,
    account_id  INTEGER NOT NULL,
    status      TEXT DEFAULT 'pending',        -- pending/processing/done/failed
    attempt     INTEGER DEFAULT 0,             -- 重试次数
    error       TEXT,
    UNIQUE(video_id)                           -- 一个URL只分给一个账号一次
);

-- 配置表（GUI 可改的全局参数）
CREATE TABLE configs (
    key   TEXT PRIMARY KEY,
    value TEXT
);
-- 示例配置项：
-- cooldown_per_batch=10       每N个URL后冷却
-- cooldown_seconds_min=60     冷却随机下限(秒)
-- cooldown_seconds_max=90     冷却随机上限(秒)
-- comment_page_interval_min=3 评论分页间隔下限(秒)
-- comment_page_interval_max=8 评论分页间隔上限(秒)
-- retry_max=3                 失败重试次数
-- retry_backoff=[30,120,300]  指数退避秒数
```

---

## 4. BitBrowser 对接方案

### 4.1 对接原理

```
BitBrowser 客户端（常驻运行）
    │ 本地 HTTP API（默认端口 54345）
    ▼
POST /api/v1/user/verify  →  验证 API Key
POST /api/v1/browser/start?profile_id=xxx  →  打开指定 profile
    │ 返回 { "ws": { "selenium": "ws://127.0.0.1:xxxx" } }（或 port 字段）
    ▼
Playwright: browser = await playwright.chromium.connect_over_cdp(ws_endpoint)
    ▼
该 profile 的指纹/代理/登录态全部生效，直接 page.goto(...) 操作
```

### 4.2 关键细节

| 项 | 说明 |
|---|---|
| API 地址 | `http://127.0.0.1:54345`（BitBrowser 本地服务，默认端口） |
| API Key | BitBrowser 设置页获取，写入 `configs` 表 |
| profile 管理 | 每账号 = 一个 BitBrowser profile（免费版 10 个） |
| 登录态 | 首次用 BitBrowser 手动登录平台账号 → 登录态保存在 profile 内，后续自动复用 |
| 打开失败 | 检测 profile 是否已打开（`browser/active`），已打开则复用或先 close |
| 异常处理 | `connect_over_cdp` 失败 → 重试打开 profile → 仍失败则账号标记 `login_expired` 并 GUI 提醒 |
| **P7 冻结（硬约束）** | 账号 `waiting_human` 时：**该 profile 窗口保持打开、页面保持现状**。系统不得调用 `browser/stop`、不得 `page.close()`、不得 `page.goto()` 刷新/导航。即使程序退出重开，也必须先恢复原 profile 连接（BitBrowser 保持窗口常驻），确认用户处理后才能继续 |

### 4.3 并发模型

- 每个账号一个独立 Playwright Browser 实例（对应一个 profile），**常驻复用**（不每任务开关，省时间）
- 5 个账号 = 5 个 browser 实例 + 5 个 page（每个实例建议单 page，降低内存）
- 打开 profile 的 API 调用串行化（BitBrowser 本地服务处理并发有限）
- **冻结例外**：`waiting_human` 的账号实例不参与「空闲清理」「超时回收」「内存优化关闭」等任何自动化回收逻辑，直到用户通知继续

---

## 5. 任务调度与冷却机制

### 5.1 两阶段流程

```
┌─ 阶段A：搜索采集（单账号）──────────────────────────────┐
│ 1. 调度器选「搜索账号」（平台绑定的第一个可用账号）        │
│ 2. 对每个关键词：search(keyword, max_pages)              │
│ 3. 逐页解析 → 去重 → 写入 videos 表（comment_status=pending）│
│ 4. 每翻页间隔随机 3~8s；搜索账号同样受冷却约束            │
└──────────────────────────┬──────────────────────────────┘
                           ▼
┌─ 阶段B：评论采集（N 账号并行）──────────────────────────┐
│ 1. 取该任务下 comment_status=pending 的 URL 列表          │
│ 2. 均分：chunk = ceil(total / N)，账号 i 分到 [i*chunk, (i+1)*chunk) │
│ 3. 每账号串行处理自己的 URL：                              │
│    a. fetch_comments(url) → 写入 comments 表              │
│    b. 已处理 URL 计数 +1                                  │
│    c. 达到 cooldown_per_batch(10) 个 → 冷却 60~90s 随机    │
│    d. 冷却后计数清零，继续下一个 URL                       │
│ 4. 全部完成 → 任务 status=done                            │
└──────────────────────────────────────────────────────────┘
```

### 5.2 均分算法（含余数处理）

```python
def split_urls(urls: list, n_accounts: int) -> dict[int, list]:
    chunk = math.ceil(len(urls) / n_accounts)
    result = {}
    for i in range(n_accounts):
        result[i] = urls[i * chunk : (i + 1) * chunk]
    return result
# 例：17 个 URL / 3 账号 → [6, 6, 5]（余数自然分布在末尾，无特殊处理）
```

### 5.3 冷却策略（防"机械规律"被识别）

| 场景 | 参数（默认） | 随机化 |
|---|---|---|
| 每批 URL 后 | 10 个 → 停 60~90s | ✅ 区间随机 |
| 评论分页之间 | 3~8s | ✅ 区间随机 |
| 搜索翻页之间 | 3~8s | ✅ 区间随机 |
| 连续失败 3 次 | 指数退避 30s→120s→300s | — |
| 每日每账号上限 | 200 个 URL（可配置） | — |
| 凌晨高峰 | 平台高峰时段（如 20~24 点）加大冷却 ×1.5 | ✅ 可配置开关 |

> ⚠️ **修正原需求**：固定"停 1 分钟"太规律，容易被风控识别。改为「60~90s 随机」，默认仍以 10 URL 为一批。

### 5.4 任务状态机

```
                 ┌────────────┐
   创建任务 ────▶ │  pending   │
                 └─────┬──────┘
                       ▼
                 ┌────────────┐   搜索失败/超时
                 │ searching  │ ──────────────▶ failed(可重试)
                 └─────┬──────┘
                       ▼ (URL全部入库)
                 ┌────────────┐
                 │ commenting │ ◀── 账号冷却中(子状态)
                 └─────┬──────┘
                       ▼ (全部URL完成)
                 ┌────────────┐
                 │    done    │
                 └────────────┘

账号级状态（accounts.status，P7 人工接管核心）：
  ready → busy → cooldown → busy → ... 
     ↘ waiting_human（P7：冻结浏览器，等待用户「继续」通知）
     ↖─────────── 用户点击 GUI「继续」后恢复 busy
```

### 5.5 失败处理

| 失败类型 | 处理 |
|---|---|
| **登录态过期（跳登录页）** | **进入 `waiting_human`**：冻结该 profile 当前页面不动，GUI 醒目提示「浏览器 dy_01 需要人工登录」，等待用户通知继续 |
| **验证码/滑块** | **进入 `waiting_human`**：冻结页面（保留验证码弹窗原样），GUI 提示「浏览器 dy_01 出现验证码，请人工处理」，等用户通知继续 |
| 网络超时 | 重试（最多 3 次，指数退避） |
| 单 URL 评论失败 | 标记 `failed`，记 error，不影响其他 URL；任务结束时汇总可手动重试 |
| 账号被踢/禁用 | 剩余 URL 重新均分给其他可用账号 |

> ⚠️ **P7 冻结规则（硬约束，任何代码路径不得违反）**：
> 1. 账号进入 `waiting_human` 后，其 profile 的浏览器**保持现状**：不刷新、不关闭、不导航、不滚动、不点击
> 2. 采集器对该账号**暂停派发新任务**；已派发的当前任务原地挂起
> 3. 只有用户点击 GUI 上的「继续」按钮（或等效通知），该账号才恢复
> 4. 恢复后从**当前 URL 断点**继续（不重头开始，避免重复请求）
> 5. 若用户关闭了程序再打开：该账号仍处于 `waiting_human`（状态已落盘），继续冻结等待用户处理

---

## 6. GUI 设计

### 6.1 页面结构

```
┌─ 主窗口 ─────────────────────────────────────────────────┐
│ 顶部导航： [任务管理] [账号管理] [配置中心] [数据导出]       │
├──────────────────────────────────────────────────────────┤
│ 任务管理页                                                 │
│   [新建任务面板]                                           │
│   平台: (抖音▾)  关键词: [________________]  [+ 添加关键词] │
│             （支持批量：粘贴多个关键词，回车分隔）            │
│   搜索账号: (dy_01▾)   评论账号: [dy_01] [dy_02] [dy_03]   │
│   搜索翻页上限: [3]   评论每批冷却: [10个/60~90s]           │
│   [▶ 开始采集] [⏸ 暂停] [■ 停止]                           │
│   [🧠 意向分析] [📤 导出CSV]  ← 采集完成后可用              │
│                                                          │
│   任务列表（实时）                                         │
│   ┌──────┬────┬───────┬────────┬────────┬──────┐        │
│   │ 任务ID│平台 │ 关键词 │ 进度    │ 状态    │ 操作  │        │
│   ├──────┼────┼───────┼────────┼────────┼──────┤        │
│   │  #12 │抖音 │ 美食   │ 47/120 │ 采集中  │ 详情 │        │
│   └──────┴────┴───────┴────────┴────────┴──────┘        │
│   [任务详情抽屉：账号实时状态/冷却倒计时/最近错误/视频列表]    │
├──────────────────────────────────────────────────────────┤
│ 账号管理页：BitBrowser profile 绑定 / 登录态检查 / 配额查看  │
│ 配置中心页：冷却参数 / 翻页上限 / 每日配额 / 代理列表         │
│           意向规则(高/中/低关键词+回复模板) 编辑             │
│ 数据导出页：按任务/平台/关键词 导出 JSON/CSV/Excel           │
│           （CSV 统一 UTF-8 BOM，见 7B 章）                  │
└──────────────────────────────────────────────────────────┘
```

### 6.2 实时监控（WebSocket 推送）

```json
// 服务端 → GUI 每秒推送
{
  "type": "task_progress",
  "task_id": 12,
  "status": "commenting",
  "total": 120, "commented": 47,
  "accounts": [
    {"name": "dy_01", "status": "cooldown", "cooldown_left": 42,
     "done": 10, "current_url": "https://..."},
    {"name": "dy_02", "status": "processing", "done": 23, "current_url": "..."},
    {"name": "dy_03", "status": "waiting_human", "reason": "captcha",
     "prompt": "浏览器 dy_03 出现验证码，请人工处理", "frozen_since": "12:03:44"}
  ]
}
```

### 6.3 人工接管交互（P7 硬约束的 GUI 呈现）

**触发**：任何账号进入 `waiting_human` 时。

**GUI 呈现（三管齐下，确保用户注意到）**：
1. **全局悬浮警示条**（顶部常驻，红色闪烁）：`⚠ 浏览器 dy_03 需要人工处理：出现验证码 — 处理完成后请点击下方「继续」`
2. **系统通知**：Windows 托盘通知 / 声音提示（可配置开关）
3. **账号状态红点**：任务列表和账号管理中该账号显示红色 `人工` 徽标，悬停显示原因和冻结时长

**交互流程**：
```
┌─ 人工接管弹窗（置顶，不可关闭）─────────────────────┐
│  ⚠ 需要人工操作                                       │
│  浏览器: dy_03（抖音）                                │
│  原因:   出现验证码 / 需要登录                        │
│  状态:   该浏览器已冻结，不会被刷新或关闭              │
│  操作:   请直接在 dy_03 浏览器窗口中完成验证/登录     │
│                                                     │
│  [✅ 我已处理完成，继续采集]  [⏸ 稍后处理（保持冻结）] │
└──────────────────────────────────────────────────────┘
```

**规则**：
- 「我已处理完成」→ 账号恢复 `busy`，从**当前 URL 断点**继续
- 「稍后处理」→ 弹窗收起为悬浮警示条，账号**继续冻结**，不派发新任务
- 用户关闭程序再打开：`waiting_human` 状态落盘恢复，警示条重新弹出，浏览器保持冻结
- **冻结期间系统绝不执行**：刷新、关闭、导航、滚动、点击该浏览器（见 5.5 节 P7 规则）

---

## 7. 采集引擎：MediaCrawler 二次开发方案

### 7.1 为什么用它

| 能力 | MediaCrawler | 本平台需要 |
|---|---|---|
| 关键词搜索 | ✅ 三平台都支持 | ✅ |
| 评论采集（含二级） | ✅ 三平台都支持 | ✅ |
| 登录态缓存 | ✅ 自动保存/复用 | ✅ |
| IP 代理池 | ✅ | ✅（可后续接入） |
| 本地已有克隆 | ✅ F:\DSH\MediaCrawler | ✅ 直接改 |

### 7.2 需要改造的点

1. **去除自带登录流程**：改为「启动时校验 BitBrowser profile 内登录态，未登录则 GUI 引导人工登录（走 P7 人工接管）」
2. **驱动替换**：默认 Playwright 自起浏览器 → 改为 `connect_over_cdp` 连接 BitBrowser profile
3. **接口化封装**：把 `main.py` 的 CLI 参数模式，封装成 `SearchCollector.search(keyword, max_pages)` / `CommentCollector.fetch(url)` 两个可编程接口
4. **回调钩子**：每采集一条视频/评论 → 回调写入我们的 SQLite + 推送 GUI
5. **风控钩子（P7 核心）**：识别到验证码/滑块/登录页 → 抛 `HumanInterventionRequired` 异常（携带账号名+原因+提示文案）→ 调度器将该账号置为 `waiting_human` 并冻结浏览器，**绝不自行重试、刷新或关闭**

---

## 7A. 意向分析模块（复用上会话成果）

> 上会话已实现完整流程并产出 CSV，本模块是其产品化封装。目标：从评论中识别「有意向的用户」，产出两个层级的导出。

### 7A.1 流程

```
评论表（已采集完成）
   │ 规则打分（intent_rules 表 / intent-rules.json）
   ▼
评论级意向表（导出1：douyin-intent-leads.csv 同款）
   用户ID | 用户主页 | 评论内容 | 意向评分 | 回复建议 | 昵称 | 时间 | 来源作品
   │ 按 uid 聚合（只留高意向，≥5分）
   ▼
用户级聚合表（导出2：douyin-user-leads.csv 同款）
   用户ID | 昵称 | 主页 | 高意向评论数 | 评论总条数 | 时间区间 | 涉及作品数 | 摘录
```

### 7A.2 打分规则（默认，GUI 可编辑）

| 级别 | 分数 | 示例关键词 | 回复模板 |
|---|---|---|---|
| 高意向 | 5 | 多少钱/价格/报价/怎么卖/购买/加盟/代理/合作/微信/电话/求购/供应商/厂家 | 厂家直销+方案定制，可报价… |
| 中意向 | 3 | 投放/怎么用/尺寸/质保/售后/对比/哪个好/成本/回本/收益 | 介绍功能/规格/方案对比… |
| 低意向 | 1 | （无命中） | 通用感谢+引导私信 |

- 命中即取最高级（5 优先于 3）；支持多关键词命中累计（可配置）
- 规则存在 `intent_rules` 表，GUI「配置中心」可增删改，不再改 JSON 文件

### 7A.3 打分时机

- **默认**：评论采集完成后，任务内一键「运行意向分析」（离线批量，不占浏览器）
- **可选**：采集过程中实时打分（写入 `intent_score` 字段），GUI 可勾选

---

## 7B. CSV 导出规范（修复上会话乱码问题）

> 上会话踩坑：`douyin-user-leads.csv` 用 `[Text.Encoding]::UTF8` 导出后表头乱码（Excel 打开 GBK 解码）；`douyin-intent-leads.csv` 用 UTF-8 BOM 则正常。**统一规则：所有导出文件用 UTF-8 BOM。**

### 7B.1 导出文件清单

| 文件 | 内容 | 表头 |
|---|---|---|
| `{task}_videos.csv` | 视频/笔记清单 | 平台,视频ID,标题,作者,发布时间,URL,评论状态 |
| `{task}_comments.csv` | 全部评论 | 用户ID,昵称,评论内容,点赞数,评论时间,来源作品,作品链接 |
| `{task}_intent_leads.csv` | 意向评论明细 | 用户ID,用户主页,评论内容,意向分析评分,回复建议,昵称,评论时间,来源作品,作品链接 |
| `{task}_user_leads.csv` | 高意向用户聚合 | 用户ID,昵称,用户主页,高意向评论数,评论总条数,最新评论时间,最早评论时间,涉及作品数,高意向评论摘录,涉及作品列表 |

### 7B.2 编码规则

- 文件编码：**UTF-8 with BOM**（`utf-8-sig`），保证 Excel 双击打开不乱码
- CSV 转义：字段含 `,` `"` `\r` `\n` 时加引号，内部 `"` 双写（复用上会话 `Csv-Escape` 逻辑）
- 换行符：`\r\n`（CRLF）
- 时间格式：`yyyy-MM-dd HH:mm`（本地时区）

---

## 8. 平台采集流程 ①：抖音（Douyin）

> 建议第一个落地。最难但也最典型，跑通后其余平台复用框架。

### 8.1 平台特性

| 项 | 现状 |
|---|---|
| 搜索是否需要登录 | ✅ 需要（Cookie 有效才能稳定翻页） |
| 搜索翻页上限 | 网页端约 2~3 页后触发验证码（`verify` 弹窗） |
| 评论是否需要登录 | ✅ 需要（匿名只能看少量） |
| 评论接口 | 网页端 API `aweme/v1/web/comment/list/`（需签名参数） |
| 签名方案 | MediaCrawler 已实现 JS 表达式取签名，无需自己逆向 |
| 主要风控 | 滑块验证码、频繁请求限流、账号关联检测 |

### 8.2 采集流程

```
阶段A-搜索（主账号）:
  search(keyword):
    goto https://www.douyin.com/search/{keyword}?type=video
    逐页下滑加载 → 每页解析 aweme_id / 标题 / 作者 / 点赞
    翻页上限 max_pages=3（默认）→ 触验证码即停
    → 写入 videos 表（platform=douyin, video_id=aweme_id）

阶段B-评论（N 账号）:
  fetch_comments(url):
    goto 视频页（或直接请求评论 API，带登录 Cookie）
    循环 cursor 翻页拉取一级评论 → 每页间隔 3~8s
    可选拉取二级评论（默认开启，深度≤2）
    → 写入 comments 表
```

### 8.3 风险与对策

| 风险 | 对策 |
|---|---|
| 滑块验证码 | 暂停该账号 + GUI 提醒人工过码；或换账号 |
| 搜索翻页少（2~3页） | 接受现实：抖音网页端搜索就是浅层；如需更深，可增加「相关推荐」或「关键词变体」（如"美食"→"美食做法"）扩充结果 |
| 登录态失效 | 账号标记 login_expired，GUI 一键唤起 BitBrowser profile 让用户重新登录 |
| 评论接口风控 | 每账号评论分页间隔随机化；每日配额限制 |

### 8.4 验收标准

- [ ] 1 个关键词能搜到 ≥2 页结果并全部入库去重
- [ ] 1 个视频的评论能完整拉取（含二级评论）
- [ ] 3 个账号并行跑 30 个 URL，冷却机制生效，无账号被风控
- [ ] 触验证码时 GUI 正确提醒、任务可恢复

### 8.5 上会话实测记录（抖音单账号 CDP 已验证）

> 来源：`dsh-session-session-c9124011-...zip` + `F:\DSH\cdp\` 产出物。这些是**已经跑通的事实**，不是推测：

| 项 | 实测情况 |
|---|---|
| 搜索方式 | 网页端 `https://www.douyin.com/search/{kw}?type=video`，滚动加载触发 `/search/` API，解析 `aweme_id` |
| 每关键词结果量 | 约 20~60 条/关键词（13 关键词合并去重后约 150+ 条，`search-results-all.json` 120KB） |
| 评论采集方式 | 打开视频页 → 滚动 `.route-scroll-container` → 点击「点击加载更多」→ 抓 `/comment/list/` API 响应体 |
| 每个视频耗时 | `PerWorkSeconds=13~14s` 滚动窗口内抓完评论 |
| 冷却实测 | `PauseEvery=10 + PauseDuration=20s`（每 10 个视频停 20 秒）全程未触发风控 ✅ |
| 意向分析 | `intent-rules.json` 打分 → 意向 CSV 产出成功（高意向命中正常） |
| 蓝屏问题 | 曾触发蓝屏；排查后为**内存 XMP 不稳定**（用户将内存频率降至 2666 后恢复）→ 本平台需注意多浏览器实例内存占用，见第 14 章 |
| 搜索翻页少 | 抖音网页端搜索翻 2~3 页即触发验证码 → **多关键词变体是扩充结果量的主要手段**（上会话用 13 个关键词），GUI 需支持批量关键词 |

**对本平台设计的启示**：
1. 单关键词结果少是平台特性，**GUI 必须支持批量关键词**（一次 5~15 个），靠关键词变体扩充覆盖面
2. 冷却「每 10 个停 20s」已被验证安全 → 新平台默认「每 10 个停 60~90s」更保守，可行
3. 评论采集的上会话实现（滚动+点击加载更多+抓 API 响应）可直接移植为 Playwright 版

---

## 9. 平台采集流程 ②：小红书（XHS）

> 风控最强平台。建议第二个落地，重点打磨反风控节奏。

### 9.1 平台特性

| 项 | 现状 |
|---|---|
| 搜索是否需要登录 | ✅ 需要（未登录搜索体验差且接口受限） |
| 搜索翻页上限 | 受限明显，频繁翻页触发滑块 |
| 内容类型 | 笔记（图文+视频混合），采集对象为「笔记」 |
| 评论接口 | 网页端 API `api/sns/web/v2/comment/page`（需签名 + Cookie） |
| 主要风控 | 滑块（`captcha`）、`xsec_token` 时效校验、账号行为分析 |

### 9.2 采集流程

```
阶段A-搜索（主账号）:
  search(keyword):
    goto https://www.xiaohongshu.com/search_result?keyword={keyword}
    下滑加载 → 解析 note_id / 标题 / 作者 / 互动数
    翻页上限 max_pages=5（默认，视风控可调）
    → 写入 videos 表（video_id=note_id）

阶段B-评论（N 账号）:
  fetch_comments(url):
    请求评论 API（带 Cookie + xsec_token）
    cursor 翻页 → 一级评论 → 二级评论（可选）
    → 写入 comments 表
```

### 9.3 风险与对策

| 风险 | 对策 |
|---|---|
| 滑块触发频繁 | 冷却参数调保守：每 5 URL 一批（默认）、分页间隔 5~10s；GUI 可调 |
| xsec_token 失效 | 采集前先从页面刷新 token；失效时重新加载页面再取 |
| 搜索限流 | 搜索账号单独配置更保守的翻页间隔；搜索阶段与评论阶段错峰（先全搜完再开评论） |
| 账号关联 | 每个 profile 独立指纹 + 独立 IP（BitBrowser 代理功能） |

### 9.4 验收标准

- [ ] 关键词搜索能稳定拉到 ≥2 页笔记（连续 3 次不触发滑块）
- [ ] 笔记评论完整拉取（含二级评论）
- [ ] 2 个账号并行 20 个 URL 全程无人工干预
- [ ] 触发滑块时能自动暂停并在 GUI 提醒

---

## 10. 平台采集流程 ③：B站（Bilibili）

> 最友好平台。建议最后落地，实际是"送分题"。

### 10.1 平台特性

| 项 | 现状 |
|---|---|
| 搜索是否需要登录 | ❌ 公开 API 即可（`api.bilibili.com/x/web-interface/search`） |
| 搜索翻页上限 | 宽松，可翻很多页（默认建议 10 页） |
| 评论是否需要登录 | ⚠️ 匿名可看但有限制；带 Cookie（SESSDATA）更稳、可看更多 |
| 评论接口 | 公开 API `x/v2/reply/main`（MediaCrawler 已支持） |
| 主要风控 | HTTP 412（频繁请求）；整体远弱于抖音/小红书 |

### 10.2 关键决策：B站要不要走浏览器？

| 方案 | 优点 | 缺点 |
|---|---|---|
| A. 直接 API（推荐） | 快、稳、不需要 BitBrowser | 需要维护 Cookie |
| B. 走 BitBrowser 浏览器 | 与抖音/小红书架构统一 | 慢、浪费 profile 资源 |

**结论：B站采集器走「API 直连模式」**——不占用 BitBrowser profile，只需一个共享 Cookie（可放配置表）。架构上仍实现统一接口 `search()` / `fetch_comments()`，内部实现不同而已。

### 10.3 采集流程

```
阶段A-搜索（API 直连，无需账号）:
  search(keyword):
    GET api.bilibili.com/x/web-interface/search?keyword={kw}&page={n}
    解析 bvid / 标题 / UP主 / 播放 / 时长
    翻页上限 max_pages=10（默认）
    → 写入 videos 表（video_id=bvid）

阶段B-评论（API 直连，共享 Cookie）:
  fetch_comments(bvid):
    GET x/v2/reply/main?type=1&oid={aid}&next={cursor}
    翻页拉取 → 一级+二级评论 → 写入 comments 表
    每页间隔 1~3s（B站宽松，可稍快）
```

### 10.4 风险与对策

| 风险 | 对策 |
|---|---|
| HTTP 412 | 降低请求频率；412 后退避 60s 重试 |
| Cookie 过期 | 复用现有 bbc-skill 的 cookie 获取流程（`get cookies.txt` 扩展导出） |
| 评论翻页深 | `reply/main` 有翻页上限，拉完为止即可 |

### 10.5 验收标准

- [ ] 关键词搜索 10 页全部入库、去重正确
- [ ] 视频评论（含二级）完整拉取
- [ ] 单日 1000+ 视频评论采集无 412 中断

---

## 11. 统一整合：完整运行流程

### 11.1 用户视角

```
1. 打开 GUI → 账号管理页：确认 5 个 BitBrowser profile 已绑定并登录对应平台账号
2. 新建任务：平台=抖音，关键词=["快递智能柜","智能快递柜","菜鸟驿站",...]（可批量粘贴 5~15 个），
   评论账号=[dy_01,dy_02,dy_03]
3. 点「开始采集」→ 调度器自动执行：
   [阶段A] dy_01 逐个搜索全部关键词 → 跨关键词按视频ID去重 → 入库 75 个URL
   [阶段B] 75 个URL 均分：dy_01=25, dy_02=25, dy_03=25
          各自每采 10 个URL 冷却 60~90s，直至全部完成
4. GUI 实时显示进度；中途某账号被验证码拦截 → 弹窗提醒，其余账号继续
5. 全部完成后：点「🧠 意向分析」→ 按 intent_rules 打分 → 高/中/低意向标注
6. 「📤 导出CSV」→ 产出 4 类文件（UTF-8 BOM）：
   视频清单 / 全部评论 / 意向评论明细 / 高意向用户聚合
   （格式与上会话 douyin-intent-leads.csv、douyin-user-leads.csv 一致）
```

### 11.2 平台×账号矩阵（示例：5 个 profile）

| Profile | 平台 | 角色 |
|---|---|---|
| dy_01 | 抖音 | 搜索账号 + 评论账号 |
| dy_02 | 抖音 | 评论账号 |
| dy_03 | 抖音 | 评论账号 |
| xhs_01 | 小红书 | 搜索 + 评论账号 |
| xhs_02 | 小红书 | 评论账号（可选） |
| bili_cookie | B站 | API 直连（共享 Cookie，不占 profile） |

> 免费版 10 个 profile：抖音 3 + 小红书 2 + B站 0 = 5 个，还有 5 个余量。

### 11.3 整合原则

- **统一任务模型**：三平台共用 tasks/videos/comments 表，platform 字段区分
- **统一采集接口**：`search(keyword, max_pages) -> List[VideoMeta]`、`fetch_comments(video_id) -> List[Comment]`
- **统一风控框架**：冷却/配额/重试在调度层统一实现，平台只负责抛异常
- **平台差异收敛在采集器内部**：抖音=浏览器、小红书=浏览器、B站=API，调度器无感知

---

## 12. 实施路线图

| 阶段 | 内容 | 交付物 | 预估 |
|---|---|---|---|
| **P0 环境** | BitBrowser 安装+profile 创建；MediaCrawler 跑通 demo；Playwright+BitBrowser CDP 连接验证；上会话脚本资产核对（`F:\DSH\cdp\`） | 连接验证脚本 + 资产清单 | 0.5 天 |
| **P1 骨架** | SQLite 建库；任务调度器（状态机+均分+冷却）；GUI 框架（任务创建+进度显示）；批量关键词支持 | 可跑通"假数据"全流程 | 2~3 天 |
| **P2 抖音** | 抖音采集器接入（搜索+评论）；BitBrowser 对接；验证码处理；移植上会话评论采集算法 | 抖音全流程可用（含意向分析+CSV导出） | 3~5 天 |
| **P3 小红书** | 小红书采集器；风控参数调优 | 小红书全流程可用 | 3~5 天 |
| **P4 B站** | B站 API 直连采集器 | B站全流程可用 | 1~2 天 |
| **P5 意向分析+导出** | `intent_rules` 表 GUI 编辑；打分引擎；4 种 CSV（UTF-8 BOM）；用户聚合 | 意向用户/用户聚合报表 | 1~2 天 |
| **P6 整合打磨** | 断点续采、异常兜底、多任务并发、内存监控（规避蓝屏）、崩溃自恢复 | 完整产品 | 2~3 天 |

> 总计约 2~3 周（单人全职）。**P2 完成即可先用起来**（抖音 + 意向分析 + CSV 导出），后续平台逐步加。

---

## 13. 合规与风险声明

1. **数据合规**：采集内容仅限公开数据，用于个人学习/分析；不得大规模商业化倒卖数据
2. **平台 ToS**：抖音/小红书/B站 用户协议均禁止未授权爬取；多账号操作有封号风险，账号损失自负
3. **工具 ToS**：BitBrowser 免费版 10 profile 用于商业用途需注意其条款；XChrome 等开源工具 CC-BY-NC 禁止商用
4. **本项目建议**：控制采集规模（日配额）、尊重 robots 与平台规则、账号使用非核心小号
5. **技术免责**：本文档描述的逆向/反爬方案仅用于技术研究，实施前请评估法律风险

---

## 14. 历史踩坑清单（上会话实测，本平台必须规避）

> 来源：`dsh-session-session-c9124011-...zip` 会话记录。每条都真实发生过。

| # | 坑 | 现象 | 根因/处理 | 本平台对策 |
|---|---|---|---|---|
| 1 | **蓝屏/内存不稳定** | 采集运行中触发蓝屏 | 内存 XMP 频率不稳定（4.36 vs 4.68），降到 2666 后恢复 | 多浏览器实例内存占用大（每 profile ≈ 300~500MB），GUI 显示内存占用；并发账号数默认 3 保守起步；提供「低内存模式」（单 page/账号） |
| 2 | **CSV 表头乱码** | `douyin-user-leads.csv` Excel 打开表头乱码 | 用无 BOM 的 UTF-8 写入，Excel 按 GBK 解码 | **所有 CSV 统一 UTF-8 BOM**（第 7B 章），导出后用 Python `utf-8-sig` 校验 |
| 3 | **搜索翻页少** | 单关键词只搜到 20~60 条，内容太少 | 抖音网页端翻 2~3 页即验证码 | 批量关键词变体扩充（上会话 13 个关键词合并 150+ 条）；GUI 支持粘贴多关键词 |
| 4 | **验证码需人工** | 搜索/评论触发滑块 | 平台反爬 | **P7 人工接管**：检测到验证码 → 账号 `waiting_human` 冻结（不刷新不关闭）→ GUI 提示是哪个浏览器 → 人工过码 → 点「继续」恢复断点，其余账号不中断 |
| 5 | **页面崩溃** | 浏览器页面崩溃中断任务 | 内存压力/长任务 | 非冻结态崩溃 → 自动重开 profile 断点续采；**冻结态（waiting_human）崩溃 → 保持等待，仅 GUI 提示「浏览器已关闭请重新打开」**，不自动操作 |
| 6 | **搜索类型选错** | 用 `type=general` 搜索出错 | 抖音搜索需指定 `type=video` | 采集器固定 `type=video`，GUI 不暴露该参数 |
| 7 | **单一窗口限制** | 用户以为应多窗口并行，实际只有一个页面 | 单账号 CDP 单页面模型 | 本平台多 profile 并行即多窗口，天然解决；GUI 展示每个账号独立窗口/页面 |
| 8 | **耗时不可控** | 每视频 13~14s，100+ 视频单账号要 30+ 分钟 | 串行采集 | 本平台 N 账号并行，理论提速 N 倍；GUI 显示预计剩余时间 |

---

## ⚠️ 附录 A 勘误（2026-08-18 P0 实测）

> 下方附录 A 是老版 BitBrowser（ads-power 系）的接口，**已过时**。本机实测 BitBrowser Global v7.1.5 的差异：

| 项 | 老版（附录 A） | Global v7.1.5 实测 |
|---|---|---|
| 鉴权 | `/api/v1/user/verify` + token | **无需鉴权**（`enableAuth=false`） |
| 路径 | `/api/v1/...` 带前缀 | **无前缀**，全部 `POST` + JSON body |
| 启动 | `/api/v1/browser/start` | `POST /browser/open` `{"id","args":[],"queue":true}` → `data.ws`（CDP WebSocket） |
| 列表 | — | `POST /browser/list` `{"page":0,"pageSize":100}` |
| 健康检查 | — | `POST /health` → `{"success":true}` |

- **本地 API 服务默认关闭**：`%APPDATA%\BitBrowser\db.sqlite` 中 `global_configs.koaServer` 置 `1` 后重启客户端即常驻（端口 `server_port`，默认 54345）。
- 完整接口字段见 [`doc-browser-api.txt`（官方文档存档）](D:\DSH\cdp\doc-browser-api.txt)；P1 模块接口契约见 [`p1-contract.md`](D:\DSH\platform\docs\p1-contract.md)。
- 坑：JSON body 必须 UTF-8 发送（PowerShell 发中文会乱码）；窗口关闭后等 5s 再删/重开。

## 附录 A：BitBrowser 本地 API 速查（老版，仅参考）

```bash
# 验证 API Key
curl http://127.0.0.1:54345/api/v1/user/verify \
  -H "Content-Type: application/json" \
  -d '{"token":"YOUR_API_KEY"}'

# 打开 profile（返回 CDP 连接信息）
curl http://127.0.0.1:54345/api/v1/browser/start \
  -H "Content-Type: application/json" \
  -d '{"profile_id":"xxx","open_urls":["https://www.douyin.com"]}'
# 响应示例: {"code":0,"data":{"ws":{"selenium":"ws://127.0.0.1:42033/devtools/browser/..."}}}

# 关闭 profile
curl http://127.0.0.1:54345/api/v1/browser/stop \
  -H "Content-Type: application/json" \
  -d '{"profile_id":"xxx"}'
```

## 附录 B：核心配置默认值

| 配置 | 默认值 | 平台备注 |
|---|---|---|
| cooldown_per_batch | 10 URL | 抖音/小红书 10；B站可不冷却 |
| cooldown_seconds | 60~90s 随机 | 小红书可调 90~150s |
| search_max_pages | 抖音3 / 小红书5 / B站10 | GUI 可覆盖 |
| comment_page_interval | 3~8s | B站 1~3s |
| daily_limit_per_account | 200 URL | GUI 可调 |
| retry_max / backoff | 3 次 / 30,120,300s | — |
| 并发账号数 | 3（可调至 5） | 受免费 profile 数限制 |
